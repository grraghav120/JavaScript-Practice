//Q1

let age = 12;

console.log(
  age >= 10 && age <= 20 ? "Yes, Age lies between 10 to 20" : "No, tullu"
);

//Q2

let num=20;

switch(num){
    case 1:
        console.log(`Bro, ${num}`);
        break;
    case 2:
        console.log(`Bro, ${num}`);
        break;
    case 3:
        console.log(`Bro , ${num}`);
        break;
    default:
        console.log("default");
}

//Q3

let num1=16;

if(num1%2===0 && num1%3===0) console.log(`${num1} is divisible by 2 & 3`);
else console.log(`${num1} is  not divisible by 2 & 3`);

//Q5

let driver_age=1;

console.log(driver_age>=18?"Yes you can drive":"No can't drive");